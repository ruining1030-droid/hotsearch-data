services:
  - type: web
    name: hotsearch-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python hotsearch_api.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
